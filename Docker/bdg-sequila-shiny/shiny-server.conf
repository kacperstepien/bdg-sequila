run_as shiny;
preserve_logs true;
app_init_timeout 6000;

server {
  listen 3838;
  location / {
    site_dir /srv/shiny-server;
	log_dir /var/log/shiny-server;
	bookmark_state_dir /var/log/shiny-server;
	log_file_mode 0644;
    directory_index on;
  }
}
